//#pragma once

const int MAX_SIZE = 50;

enum Months { JAN, FEB, MAR, APR, MAY, JUN, JUL, AUG, SEPT, OCT, NOV, DEC };
enum Gender { MALE, FEMALE };
enum Y_N {YES,NO};
enum MaritalStat { MARRIED, SINGLE, DIVORCED, WINDOWED, SEP };
enum cMethod{ HOME, WORK, EMAIL };
enum Relationship { FAMILY, FRIEND, CLOSE };